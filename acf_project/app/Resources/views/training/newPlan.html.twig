{% extends'base.html.twig' %}

{% block body %}
    {{ parent() }}

    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>New Lesson Plan</h1>
                <h2>{{ title }}</h2>
                {{ form_start(form) }}
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-success">Save <span class="fa fa-lock"></span></button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}